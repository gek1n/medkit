# Family subsystem — archived 10.08.2026

Цей код винесено з `lib/` (Крок 10 плану "довести Сім'ю до робочого стану",
`C:\Users\user\.claude\plans\nested-percolating-metcalfe.md`), а не видалено —
пірінг/relay-протокол, гранти видимості, "перекладач" кешу чужих даних у
звичайні картки Сьогодні/Розкладу/Медкартки — все це переважно
перевикористовне для нової серверної архітектури (Крок 11 того ж плану).

## Чому

За один день (10.08) знайшлись і виправлені 5 окремих реальних багів у
peer-to-peer relay-синку (порядок скасування/планування сповіщень;
заморожений статус на пристрої піра; відсутня гілка в розрахунку
"пропущено"; неправильний грант-бар'єр для нагадувань; плюс ще
недосліджені — плюшки підписки не поширюються, взаємна невидимість
завдань між пристроями). Кожен фікс відкривав наступний — архітектура
(relay-сервер як сліпий поштовий ящик, автопредставлення, обмін
ключами, локальний кеш чужих даних) визнана занадто крихкою для
подальшого латання. Рішення: заморозити застосунок на "тільки локальні
профілі" як нову стабільну базу, а сім'ю переробити з сервером як
джерелом правди.

## Що НЕ архівовано (лишилось у `lib/`, працює як і раніше)

- `plans_screen.dart`, `subscription_service.dart`,
  `subscription_api_client.dart`, `plan_provider.dart` (весь
  `AppPlan`/`PlanLimits`) — екран "Тарифи" лишається повністю робочим,
  бо є реальні платні користувачі з активною Family-підпискою.
- `elly_denied_screen.dart` — це загальний віджет "ліміт тарифу
  вичерпано", використовується в багатьох локальних екранах (додавання
  ліків/завдань/розділів тощо), НЕ лише в сімейному контексті.
- Схема БД (`app_database.dart`) — 100% як є, без нової міграції.
  Сімейні таблиці (`FamilyPeers`, `FamilyGrants`, `SharedSubjects`,
  `SharedEntities`, `KnownFamilyMembers`, `PendingGroupInvites`,
  `SharedChannels`) лишаються зареєстрованими в схемі, файли їхніх
  визначень також скопійовані сюди для довідки (`db/tables/`) — самі
  таблиці просто порожні для локальних користувачів.
- Власний хмарний бекап акаунту (`account_service.dart`,
  `sync_service.dart` тощо) — третій, окремий механізм (відновлення
  СВОЇХ даних на новому телефоні), не сімейний sync.

## Структура

Дзеркалить оригінальні шляхи в `lib/`:

- `services/` — з `lib/core/services/`: relay/pairing-крипто, group-service,
  visibility-service, peer-sync-service, grace-period/розпад сім'ї.
- `providers/` — `real_plan_provider.dart` (СТАРА версія, з
  "подарованим сім'єю" планом; актуальна спрощена версія лишилась у
  `lib/core/providers/`, дивись Крок 10.3 плану).
- `features/family/`, `features/profile/`, `features/onboarding/`,
  `features/plans/`, `features/today/widgets/` — екрани/провайдери.
- `widgets/` — з `lib/shared/widgets/`: peer-специфічні картки.
- `db/tables/` — визначення таблиць (лишились зареєстровані в схемі,
  копія тут лише для довідки).
- `repositories/` — `family_peers_repository.dart`,
  `shared_channels_repository.dart`.

## Бекенд

`medkit-backend` цього разу НЕ чіпали — Relay/Billing-ендпоінти
(`medkit_private/src/Modules/Relay/*`, `Billing/*`, міграції
`004_family_sync.sql`, `005_billing.sql`) просто перестають отримувати
запити з нового клієнтського білда. Мертвий код на сервері, нічим не
заважає, легко реактивувати чи видалити окремим кроком пізніше.

## Наступний крок

Крок 11 плану — нова серверна архітектура (сервер як джерело правди,
`GET /family/status` для миттєвої перевірки підписки при кожному вході
замість "просочування" через пірів, вкладення на вимогу). Велика
частина логіки тут (яка гілка синку які поля пушить, як фільтрувати за
грантами, "перекладач" кешу в картки) перевикористовна майже без змін
— міняється лише транспорт (сервер замість relay).
